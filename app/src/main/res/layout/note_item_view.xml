<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/margin_card_view"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/custom_blue"
    app:cardCornerRadius="@dimen/radius"
    app:contentPadding="@dimen/padding_content">


<LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_title_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_view_text_size"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/text_description_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_view_description_text_size"
            android:textStyle="italic" />
    </LinearLayout>

</androidx.cardview.widget.CardView>